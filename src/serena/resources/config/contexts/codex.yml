description: Codex Non-symbolic editing tools and general shell tool are excluded
prompt: |
  Codex IDE mode. File ops, edits, reads, and shell are handled by your internal tools.
  Prioritize Serena's symbolic tools for token-efficient code exploration. Avoid reading entire files.

excluded_tools:
  - replace_content


tool_description_overrides: {}

